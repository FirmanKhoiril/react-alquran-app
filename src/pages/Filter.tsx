import { Box } from "@mui/material";
import { SearchBar, SearchResult } from "../components";

const Filter = () => {
  return (
    <Box>
      Filter
      <SearchBar />
      <SearchResult />
    </Box>
  );
};

export default Filter;
